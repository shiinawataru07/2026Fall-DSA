# 2026.9.16 DSA 实验班上机练习
## A:Familiar？
我们能够通过给出的 $c_1, c_2, \ldots, c_n$ 来还原出第几次插入的元素去了哪个位置。

因为每次插入操作都会将新插入的元素放在堆的最后一个位置，而其父节点的位置也是确定的。

而最终堆是已知的，因此我们可以还原出一个候选插入序列，按照每次插入的对应交换次数使得最终得到最终堆。

最终我们检验候选插入序列插入的每个数是否能合法得到对应的交换次数即可。